# PortScanner-Cpp

Multithreaded TCP Port Scanner in C++

Scans all TCP ports on a given IP using C++ sockets and multithreading.

Features:
- Windows Winsock socket API
- `std::thread` for fast parallel scanning
- Clean console output with mutex
